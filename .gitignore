# Raw scraped data — too large for git (38MB), regenerate with: node scrape_mal.js
mal_anime.ndjson
*.ndjson

# Scraper progress checkpoint
.scrape_progress.json

# Server-mode Brotli payloads — not needed in the repo
main.json.br
main.bin.br
display.json.br

# Old JSON payload (replaced by binary format)
main.json.gz

# Claude Code local settings
.claude/

# macOS
.DS_Store
